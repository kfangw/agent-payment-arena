"""What a run is scored on.

Loss from unauthorized payments, over-blocking of legitimate tasks,
escalations raised, token cost, and wall-clock latency. Reporting any one of
these alone is misleading, so the report always carries the whole vector.
"""

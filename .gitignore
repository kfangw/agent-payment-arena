# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Run artifacts. Cassettes are committed on purpose (CI replays them); the
# generated reports and traces from a local run are not.
runs/
*.otlp.jsonl

# Local secrets
.env

_to_delete/
*.md.local

# Generated experiment outputs and local run artifacts. These are produced by
# the duel runs (regenerable from the declared seeds) and are not committed;
# only the code and the declared parameters are.
results/
results_inject/
results_s2/
results_r4/
results_t1/
results_t2/
tables/
gate_report.json
run_env.sh
log_*.txt

# Local analysis reports (kept out of the repository text)
reports/

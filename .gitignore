# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Run artifacts. Cassettes are committed on purpose (CI replays them); the
# generated reports and traces from a local run are not.
runs/
*.otlp.jsonl

# Local secrets
.env
